<% content_for :head do %>
  <link rel="alternate" 
    type="application/rss+xml" 
    title="MyQuotable: Recent Member's Quotes" 
    href="<%= formatted_root_path(:rss) %>" />
<% end %>

<% content_for :pagename do %>
 Home
<% end %>

<% if @quotes.empty? %>
  There are no recent quotes to view. 
  <%= link_to "Add a quote now", new_user_quote_path(current_user) if logged_in? %>
  <%= link_to "Log in or Create an Account", login_path unless logged_in? %>
  to get started!
<% end %>

<%= render :partial => "quotes/quote_container" %>
